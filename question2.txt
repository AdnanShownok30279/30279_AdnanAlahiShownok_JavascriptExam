a)


APT-9001
Sylhet
3
because appointment id was primitive data and draft was a shallow copy 
  b)


  cloneAppointment= stucturedClone(appointmentsnapShot);

  c)function getTopThreeTests(tests) {
return tests.sort((a, b) => a.price - b.price).slice(0, 3);
}
 now the sorting function will work fine ..

 d)

 it will sort the array
in line y it will trow error because it is changing the const 
the datatype const cannot be modified after declaration